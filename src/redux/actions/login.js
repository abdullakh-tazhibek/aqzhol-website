export const addPhoneNumber = (phoneNumber) => ({
  type: "ADD_PHONE_NUMBER",
  payload: phoneNumber,
});

export const addPassword = (password) => ({
  type: "ADD_PASSWORD",
  payload: password,
});
